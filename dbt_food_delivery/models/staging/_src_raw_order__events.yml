version: 2

sources:
  - name: raw
    database: food_db
    schema: raw
    tables:
      - name: raw_order
        config: 
          freshness:
            warn_after:
              count: 1
              period: day
            error_after:
              count: 1
              period: day
          loaded_at_field: ingestion_timestamp